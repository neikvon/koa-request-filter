{
  "name": "koa-request-filter",
  "version": "1.0.1",
  "description": "request filter middleware for koa 2+",
  "main": "index.js",
  "scripts": {
    "test": "xo && ava --verbose",
    "example": "DEBUG=koa-filter node example/index.js",
    "publish-patch": "npm version patch && git push && git push --tags && npm publish",
    "publish-minor": "npm version minor && git push && git push --tags && npm publish",
    "publish-major": "npm version major && git push && git push --tags && npm publish"
  },
  "keywords": [
    "middleware",
    "koa",
    "request",
    "filter",
    "cookie",
    "ip"
  ],
  "author": "shawnnxiao",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.24.0",
    "ava-http": "^1.0.0",
    "debug": "^3.1.0",
    "koa": "^2.4.1",
    "koa-router": "^7.3.0",
    "portfinder": "^1.0.13",
    "supertest": "^3.0.0",
    "xo": "^0.18.2"
  },
  "xo": {
    "esnext": true,
    "globals": [],
    "ignores": [],
    "plugins": [],
    "semicolon": false,
    "space": 2,
    "rules": {
      "operator-linebreak": [
        0
      ],
      "no-await-in-loop": 0
    }
  },
  "dependencies": {
    "ip-filter": "^2.0.0"
  }
}
